spring:
  application:
    name: common-product
  cloud:
    nacos:
      config:
        group: ${group}
        server-addr: ${server-addr}
        file-extension: yaml
        namespace: ${namespace}
        ext‐config[0]:
          group: ${group}
          data‐id: common-product-api.yaml
          refresh: true
      discovery:
        group: ${group}
        namespace: ${namespace}
        server-addr: ${server-addr}
